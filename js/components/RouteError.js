const RouteErrorComponent = {
  render: () => {
    return `
          <section>
            <h1 class="error">Error</h1>
            <p class="error">Route does not exist!!</p>
          </section>
        `;
  },
};

export default RouteErrorComponent;
